<#
	if(!is_null(connection))
		if(!is_null(_get["id_Client"]) && local_user_info["access"] == 1)
			query(connection, "DELETE FROM users WHERE idUsers = '" + _get["id_Client"] + "'")
		if(!is_null(_get["id_Realty"]))
			query(connection, "DELETE FROM realty WHERE idRealty = '" + _get["id_Realty"] + "'")
		if(!is_null(_get["id_Order"]))
			query(connection, "DELETE FROM orders WHERE idOrders = '" + _get["id_Order"] + "'")

	redirect("/index.lgs")
#>